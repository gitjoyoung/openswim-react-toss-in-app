# ── 의존성 ──
node_modules/
.pnp
.pnp.js

# ── 빌드 산출물 ──
dist/
dist-ssr/
build/
*.local
openswim.ait
*.ait

# ── 환경 변수 (시크릿) ──
# 실제 키가 담긴 파일은 커밋 금지. 키 이름만 담은 .env.example 은 허용.
.env
.env.*
!.env.example

# ── 캐시 ──
.vite/
.cache/
.eslintcache
*.tsbuildinfo

# ── AppsInToss / Granite 빌드 상태 ──
.granite/

# ── 로그 ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── 에디터 / OS ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# ── Claude Code / 스킬 툴링 ──
.agents/
skills-lock.json
